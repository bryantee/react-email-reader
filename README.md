# React Email Reader

1. `yarn install && yarn dev` to install dependencies and run the dev environment
2. By default server runs on port `8080`
3. Using the example emailbox visit `http://127.0.0.1:8080/#/inbox`

